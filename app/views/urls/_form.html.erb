<div>
  <%= form_for @form, url: urls_path do |f| %>
    <%= f.text_field :original_url %>

    <%= f.submit "Shorten" %>
  <% end %>
</div>

<div>
  <%= shortened_url(@url) %>
</div>

<div>
  <%= link_to "Popular URLs", popular_urls_path %>
</div>
